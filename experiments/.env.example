ANTHROPIC_API_KEY=sk-ant-...

# Optional, but recommended: a free Hugging Face token (huggingface.co/settings/tokens,
# "Read" scope is enough). Anonymous downloads of large model files are bandwidth-throttled;
# an authenticated request generally isn't. Picked up automatically by transformers/
# huggingface_hub once set here (see config.py's load_dotenv()).
HF_TOKEN=hf_...
